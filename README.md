# safe-run
safe_run is a CLI tool that acts as an intelligent shell command wrapper, using an LLM (OpenAI or Ollama) to explain what any shell command does in plain English before executing it. It automatically flags potentially dangerous operations (file deletions, permission changes, network requests, etc.) with color-coded risk levels, and requires explici
